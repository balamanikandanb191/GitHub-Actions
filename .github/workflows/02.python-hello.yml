name: Run hello
on: push

jobs:
    Hello:
     runs-on: ubuntu-latest


     steps:
      - name: CLone repo
        uses: actions/checkout@v3
      - name: Setup py
        uses: actions/setup-python@v4
        with:
            python-version: '3.10'

      - name: Run a py 
        run: python hemllo.py

